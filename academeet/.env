# Example .env (DO NOT keep production credentials in the repository)
# Copy `.env.example` to `.env` and set USE_ENV_FILE=1 if you want to enable loading this file locally.
# USE_ENV_FILE=1
# DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DBNAME
